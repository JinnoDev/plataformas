# MongoDB
MONGODB_URI=mongodb://localhost:27017/socialconnect

# JWT — IMPORTANTE: usa siempre el mismo valor, si cambia se invalidan todas las sesiones
JWT_SECRET=mi_secreto_super_seguro_que_no_cambia
JWT_EXPIRES_IN=7d

# Puerto
PORT=3000
NODE_ENV=development

# (Opcional) Carpeta de subidas — por defecto: C:\socialconnect-uploads en Windows
# UPLOADS_DIR=C:\mis-uploads
