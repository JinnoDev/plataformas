import React, { type ReactNode } from 'react';
type HintRowProps = {
    title?: string;
    hint?: ReactNode;
};
export declare function HintRow({ title, hint }: HintRowProps): React.JSX.Element;
export {};
